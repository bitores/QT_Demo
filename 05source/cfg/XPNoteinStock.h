#ifndef XML_PARAM_NOTEIN_STOCK_H
#define XML_PARAM_NOTEIN_STOCK_H

#include <QObject>

class CXPNoteinStock : public QObject
{
    Q_OBJECT
public:
    explicit CXPNoteinStock(QObject *parent = 0);
    
signals:
    
public slots:
    
};

#endif // XML_PARAM_NOTEIN_STOCK_H
